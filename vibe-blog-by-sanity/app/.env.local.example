# =====================================================
# vibe-product-blog-by-sanity 環境変数設定ファイル
# =====================================================
# このファイルを .env.local にコピーして使用してください：
# cp .env.local.example .env.local
#
# Sanityの設定方法については以下を参照してください：
# https://www.sanity.io/docs/environment-variables
# =====================================================

# =====================================================
# Sanity設定（サーバーサイド）
# =====================================================
# Sanityプロジェクトの設定値を入力してください

# SanityプロジェクトID
# 取得方法:
# 1. https://www.sanity.io/manage にアクセス
# 2. 対象のプロジェクトを選択
# 3. プロジェクトIDをコピー
SANITY_PROJECT_ID=your_project_id

# Sanityデータセット
# 通常は "production" を使用
# 開発環境用に別のデータセットを作成することも可能
SANITY_DATASET=production

# Sanity APIバージョン
# 最新のAPIバージョンを使用することを推奨
# 形式: YYYY-MM-DD
SANITY_API_VERSION=2024-01-01

# =====================================================
# Sanity設定（クライアントサイド）
# =====================================================
# Next.jsのクライアントサイドで使用するため、
# NEXT_PUBLIC_プレフィックスをつけた同じ値を設定

# SanityプロジェクトID（クライアントサイド用）
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id

# Sanityデータセット（クライアントサイド用）
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity APIバージョン（クライアントサイド用）
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# =====================================================
# Sanity APIトークン（インポートツール用）
# =====================================================
# contentsディレクトリからSanityにデータをインポートする際に必要
# 
# 取得方法:
# 1. https://www.sanity.io/manage にアクセス
# 2. 対象のプロジェクトを選択
# 3. API → Tokens を選択
# 4. "Add API token" をクリック
# 5. 名前を入力（例: "Content Import"）
# 6. 権限は "Editor" 以上を選択
# 7. 生成されたトークンをコピー
#
# 注意: このトークンは秘密情報です。GitHubにコミットしないでください。
SANITY_API_TOKEN=your_api_token_here

# =====================================================
# 設定完了チェックリスト
# =====================================================
# □ Sanityプロジェクトを作成した
# □ プロジェクトIDを取得した
# □ 必要に応じて開発用データセットを作成した
# □ サーバーサイドとクライアントサイドの両方の環境変数を設定した
# □ インポート用のAPIトークンを作成した（make import-contents を使用する場合）
# =====================================================